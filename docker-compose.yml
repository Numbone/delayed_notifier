version: "3.9"

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: notifier
    ports:
      - "5488:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  rabbitmq:
    image: rabbitmq:3.13-management-alpine
    ports:
      - "5672:5672"
      - "15672:15672"
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    volumes:
      - rabbitmqdata:/var/lib/rabbitmq
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "check_port_connectivity"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    ports:
      - "6380:6379"
    volumes:
      - redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  mailhog:
    image: mailhog/mailhog:latest
    ports:
      - "1025:1025"
      - "8025:8025"

#  server:
#    build:
#      context: .
#      dockerfile: Dockerfile
#      target: server
#    ports:
#      - "8080:8080"
#    environment:
#      SERVER_PORT: "8080"
#      DATABASE_URL: "postgres://postgres:postgres@postgres:5432/notifier?sslmode=disable"
#      RABBITMQ_URL: "amqp://guest:guest@rabbitmq:5672/"
#      REDIS_ADDR: "redis:6379"
#      SMTP_HOST: "mailhog"
#      SMTP_PORT: "1025"
#    depends_on:
#      postgres:
#        condition: service_healthy
#      rabbitmq:
#        condition: service_healthy
#      redis:
#        condition: service_healthy
#
#  worker:
#    build:
#      context: .
#      dockerfile: Dockerfile
#      target: worker
#    environment:
#      DATABASE_URL: "postgres://postgres:postgres@postgres:5432/notifier?sslmode=disable"
#      RABBITMQ_URL: "amqp://guest:guest@rabbitmq:5672/"
#      REDIS_ADDR: "redis:6379"
#      SMTP_HOST: "mailhog"
#      SMTP_PORT: "1025"
#    depends_on:
#      postgres:
#        condition: service_healthy
#      rabbitmq:
#        condition: service_healthy
#      redis:
#        condition: service_healthy

volumes:
  pgdata:
  rabbitmqdata:
  redisdata:
